Place a game-over sound file here named `gameover.mp3` (or `gameover.wav`).

If present the game will attempt to use `/assets/gameover.mp3` as a fallback for game-over sound.

Alternatively use the "Upload SFX" button in the HUD to load a custom audio file at runtime.